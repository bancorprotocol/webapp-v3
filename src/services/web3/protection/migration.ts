import { PoolToken } from 'services/observables/pools';
import { ProtectedPosition } from './positions';

export const migrateV2Positions = (positions: ProtectedPosition[]) => {};

export const migrateV1Positions = (poolToken: PoolToken) => {};
