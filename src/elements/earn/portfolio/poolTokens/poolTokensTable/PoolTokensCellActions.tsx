export const PoolTokensCellActions = (converter: string) => {
  return (
    <button
      onClick={() => {}}
      className="flex items-center text-primary font-medium text-20"
    >
      Remove Liquidity
    </button>
  );
};
